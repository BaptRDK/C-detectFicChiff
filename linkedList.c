

struct linkedList
{
	char *name = NULL;
	int count = 0;
	struct linkedList *next = NULL;
};

struct test_struct *head = NULL;
struct test_struct *curr = NULL;

struct linkedList* createList(char* name)
{

}
